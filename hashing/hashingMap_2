
#include<bits/stdc++.h>
using namespace  std;

int main(){
    int num[]={2,4,3,2,3,4,5,6,};


map<int,int> hash_map;

int n = sizeof(num)/sizeof(num[0]);
for(int i = 0; i < n; i++){
    hash_map[num[i]]++;
}


// iterate over the map
// for (auto i : hash_map){
//     cout << i.first << " ->" << i.second << endl;
// }
    int q;
    cout << "Enter number of queries: ";
    cin >> q;

    while (q--) {
        int number;
        cout << "Enter the number: ";
        cin >> number;

        // Fetch frequency
        cout << number << " -> " << hash_map[number] << endl;
    }
 return 0;
}

/